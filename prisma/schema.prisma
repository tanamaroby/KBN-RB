generator client {
  provider = "prisma-client-js"
  output   = "../generated/prisma"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model users {
  email      String  @id
  password   String
  first_name String
  last_name  String
  role       String
  avatar     String?
}
